server:
  port: 8762
spring:
  application:
    name: nativeconfigserver
  # 配置文件的获取方式:native 本地
  profiles:
    active: native
  # 本地配置文件存储路径
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared